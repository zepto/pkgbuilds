# Contributor: Josiah
# This PKGBUILD file was autogenerated and should be verified before use!
pkgname=cgkit
pkgver=2.0.0alpha9
pkgrel=1
pkgdesc="Python Computer Graphics Kit"
arch=('i686')
url="http://cgkit.sourceforge.net"
license=('LGPL')
groups=('')
depends=('python')
#source=(cgkit-$pkgver.tar.gz)
source=("http://downloads.sourceforge.net/project/cgkit/cgkit/cgkit-2.0.0alpha9/$pkgname-$pkgver.tar.gz")
md5sums=('f7736958a2973467d8633b946c0ec744')

build() {
  cd $startdir/src/cgkit-$pkgver
  cd supportlib
  scons
  cd ..
  python setup.py install -O1 --prefix=/usr --root $pkgdir
}
# vim: set ts=2 sw=2 et:
